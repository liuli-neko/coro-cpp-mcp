#pragma once

#define CCMCP_VERSION_STRING "${VERSION}"
#define CCMCP_VERSION_MAJOR ${VERSION_MAJOR}
#define CCMCP_VERSION_MINOR ${VERSION_MINOR}
#define CCMCP_VERSION_PATCH ${VERSION_ALTER}
#define CCMCP_VERSION_BUILD ${VERSION_BUILD}

#define CCMCP_GIT_COMMIT "${GIT_COMMIT}"
#define CCMCP_GIT_COMMIT_LONG "${GIT_COMMIT_LONG}"
#define CCMCP_GIT_COMMIT_DATE "${GIT_COMMIT_DATE}"
#define CCMCP_GIT_BRANCH "${GIT_BRANCH}"
#define CCMCP_GIT_TAG "${GIT_TAG}"
#define CCMCP_GIT_TAG_LONG "${GIT_TAG_LONG}"

#define CCMCP_LEGAL_COPYRIGHT "${LEGAL_COPYRIGHT}"
#define CCMCP_PROJECT_NAME "${PROJECT_NAME}"
#define CCMCP_NAMESPACE  ${CCMCP_NAMESPACE}
#define LATEST_PROTOCOL_VERSION ${LATEST_PROTOCOL_VERSION}